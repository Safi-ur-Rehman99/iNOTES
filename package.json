{
  "name": "inotes",
  "version": "1.0.0",
  "description": "iNOTES - Full Stack Notes App",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client": "npm start --workspace=client",
    "server": "npm run dev --workspace=server",
    "arise": "concurrently \"npm run server\" \"npm run client\"",
    "start:prod": "concurrently \"npm start --workspace=server\" \"npm start --workspace=client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "mongodb": "^6.21.0"
  }
}
